# Public — embedded in the browser bundle
NEXT_PUBLIC_PRIVY_APP_ID=REPLACE_ME
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_USE_REAL_BLOCKCHAIN=false

# Runtime mode — read at Next build/runtime
NODE_ENV=development
